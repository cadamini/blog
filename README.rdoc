Simple rails blog for learning purposes. 

To start it just run: 

- bundle install 
- rails db:create db:migrate
- run rake createadmin
- puma

Then the website is avaiable via localhost:3000.

Default admin account (created by rake task):

admin@example.com
mypassword