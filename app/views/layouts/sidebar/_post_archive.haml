- months.each do |month|
  - if archive.keys.include?(month)
    %b= link_to Date::MONTHNAMES[month] + "(#{archive[month].size})", posts_by_month_post_path(id: archive[month], :month => month)
  - else
    = Date::MONTHNAMES[month]
  %br
