%h2
  Categories
  %small= link_to '(new)', new_category_path

%table.table.table-striped.sortable{:cellspacing => "0", :width => "100%"}
  %thead
    %tr
      %th Id
      %th Name

  %tbody
    - @categories.each do |category|
      %tr
        %td= category.id
        %td= link_to category.name, category_path(category)
        %td
          = link_to "edit", edit_category_path(category)
          = link_to 'delete', category, :method => :delete, data: { confirm: "Are you sure?" }

