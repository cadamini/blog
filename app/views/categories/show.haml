

.row
  .col-md-12
    .post

      %h3="Filter:"
      = "#{@category.name.capitalize}"
      %small= link_to '(entfernen)', posts_path

      - @posts.each do |post|

        %h3= link_to post.title, post_path(post.id)
        %p.subline
          = post.created_at.strftime("%Y-%m-%d")
          = " | #{post.comments.count} Kommentare"

